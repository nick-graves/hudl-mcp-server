HUDL_EMAIL=your-hudl-email@example.com
HUDL_PASSWORD=your-hudl-password
HUDL_TEAM_ID=your-team-id

# Optional: override the cache directory (defaults to .cache/ inside the MCP server folder)
# HUDL_CACHE_DIR=C:/path/to/custom/cache
